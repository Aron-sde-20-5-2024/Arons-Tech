import Fountation

class Music {
    init (id: Int, title: String, singer: String, image: String) {
        self.id = id
        self.title = title
        self.singer = singer
        self.image = image
    }
    
    let id: Int
    let title: String
    let singer: String
    let image: String
}
